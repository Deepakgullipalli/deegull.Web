import { FilterCheckboxListPipe } from './filter-checkbox-list.pipe';

describe('FilterCheckboxListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCheckboxListPipe();
    expect(pipe).toBeTruthy();
  });
});
